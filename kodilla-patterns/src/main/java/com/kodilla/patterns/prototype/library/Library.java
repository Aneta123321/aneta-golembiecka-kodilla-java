package com.kodilla.patterns.prototype.library;

import java.util.HashSet;
import java.util.Set;

/*public final class Library extends Prototype<Library> {

        final String name; // nazwa biblioteki z ksiazkami
        final Set<Book> books = new HashSet<>(); // po co importuje obiekt?musze wiedziec, z ktorej bibliotekiu importuje. czyli obiekt typu set jest z biblioteki utils i ja go importuje .
// w linijce 9 mam zestaw ksiazek
        public Library(final String name) {
            this.name = name;
        }

        public String getName() {
            return name;
        }

        public Set<Book> getBooks() {
            return books;
        }

    public void addBook(Book book) {
            this.books.remove(book);
    }
    public void removeBook(Book book) {
            this.books.remove(book);
    }
    }
*/
